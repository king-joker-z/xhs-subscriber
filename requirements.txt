# Python >= 3.12 required (XHS-Downloader requirement)
# XHS-Downloader is included as a git submodule at vendor/XHS-Downloader/
# Install submodule deps separately: pip install -r vendor/XHS-Downloader/requirements.txt
# 签名由 xhshow 库完成（纯 Python HTTP 签名，无需浏览器/Playwright）

# ---- Web framework & server ----
fastapi>=0.111.0
uvicorn>=0.30.0

# ---- Async HTTP client ----
httpx[http2]>=0.27.0

# ---- Scheduler ----
APScheduler>=3.10.0

# ---- Config ----
pydantic-settings>=2.0.0
PyYAML>=6.0

# ---- Database ----
aiosqlite>=0.20.0

# ---- Retry ----
tenacity>=8.0.0

# ---- XML / NFO generation ----
lxml>=5.0.0

# ---- XHS API 签名（替代 Playwright，纯 Python 实现） ----
# 同时兼容 0.1.x（sign_xs_*）和 0.2.x（sign_headers_* 完整请求头 API）
xhshow>=0.1.0,<0.3.0
